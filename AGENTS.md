## Learned User Preferences

- Merge work via feature branches and Pull Requests; do not push directly to `main` (protected: PR required, at least one approval).
- When uploading docs or assets to GitHub, use branch → commit → PR → merge rather than pushing straight to `main`.
- Style tagging UX: product detail shows small Chinese tags (`displayTags`); filters and the style library use only the eight color families (色系); optional `colorDetail` is display-only and must not drive filtering.
- Try-on should fuse the style reference image with the hand photo (DashScope / 万相), changing nail color only—not rely on text-only style descriptions.
- Prefer WeChat Cloud Development over self-hosted FastAPI/PostgreSQL for backend delivery speed and team workflow.
- Use CodeGraph (`codegraph_*` MCP) for structural code questions (callers, flows, symbols) in this workspace before grep/read loops.
- Keep evaluation hand photos available while adding camera/album upload when needed; compile and deploy the mini program with WeChat DevTools, not Cursor alone.
- Communicate in Chinese when explaining project decisions and walkthroughs.

## Learned Workspace Facts

- NailMirror: WeChat mini program for nail try-on + merchant ops; application root is `nailmirror/src/` (open this folder in WeChat DevTools).
- Team GitHub remote is `Icarus-2001/nailmirror-v1.0.3-20260527` (successor to earlier `nailmirror-v1.6-*` remotes).
- Shared cloud environment ID `cloud1-d2g3df4y16873034b` in `nailmirror/src/config/cloud-env.js`; teammates deploying to the same env share live cloud functions, DB, and env vars without local file sync.
- Collaborator backend lives on branch `backend/nail-ops` in the v1.0.3 repo (`cloudfunctions/ops`); not merged into this workspace’s `main` until PR/cherry-pick; B端 `pages-b/` still mostly Mock until wired to `ops`.
- Production backend direction (per `docs/后端及运营优化方案0527.md`): WeChat Cloud only—`tryon` deployed; planned `ops` + cloud DB collections; FastAPI/PostgreSQL out of scope before 6.6.
- Real catalog: 25 styles in `nailmirror/src/mock/styles.real.js`, generated by `scripts/import-styles.js`; default tags rotate unless run with `--vlm` / `--retag` and `DASHSCOPE_API_KEY`.
- Canonical closed tag vocabulary is `docs/美甲标签与标准词表.md` (8 color families, design, shape, style); VLM tagging via `config/tag-vocabulary.js` + `scripts/import-styles.js --vlm --retag`.
- Try-on pipeline: `cloudfunctions/tryon` (Qwen-VL for prompts/masks, 万相 e.g. `wan2.7-image-pro` / `wan2.1-imageedit` for inpaint); keys via cloud env `DASHSCOPE_API_KEY`.
- Collaboration docs: `docs/COLLABORATION_SOP.md`, `docs/GITHUB_COLLABORATION.md`, `docs/TEAMMATE_ONBOARDING.md`.
