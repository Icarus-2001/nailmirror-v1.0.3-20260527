/* V1.6 主页样式 — 自包含，不依赖外部 cover 图 */
.home {
  width: 100%;
  min-height: 100vh;
  padding: 24rpx 24rpx 100rpx;
  background: #f7f7fb;
  box-sizing: border-box;
  overflow-x: hidden;
}

.home__hero { padding: 16rpx 8rpx 24rpx; }
.home__greet { font-size: 40rpx; font-weight: 700; color: #1a1a1a; margin-bottom: 8rpx; }
.home__slogan { font-size: 28rpx; color: #6f6f7a; letter-spacing: 2rpx; }

/* 主入口卡 */
.home__main-card {
  position: relative;
  overflow: hidden;
  background: #f8f5ff;
  border: 1rpx solid rgba(155,108,255,0.14);
  border-radius: 32rpx;
  padding: 36rpx 32rpx;
  box-shadow: 0 18rpx 46rpx rgba(31,31,41,0.08);
  margin-bottom: 24rpx;
}
.home__main-card--hover { opacity: 0.92; }
.home__main-bg {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 170%;
  height: 170%;
  transform: translate(-50%, -50%);
  opacity: 0.2;
  z-index: 0;
  pointer-events: none;
}
.home__main-mask {
  position: absolute;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  z-index: 1;
  pointer-events: none;
  background: linear-gradient(
    135deg,
    rgba(248, 242, 255, 0.94) 0%,
    rgba(255, 255, 255, 0.84) 45%,
    rgba(241, 234, 255, 0.9) 100%
  );
}
.home__main-inner { position: relative; z-index: 2; display: flex; align-items: center; }
.home__main-icon {
  width: 96rpx; height: 96rpx; border-radius: 28rpx;
  background: linear-gradient(135deg, rgba(255,94,138,0.12), rgba(155,108,255,0.14));
  display: flex; align-items: center; justify-content: center;
  font-size: 52rpx; margin-right: 24rpx;
}
.home__main-text { flex: 1; min-width: 0; color: var(--np-text); }
.home__main-title { font-size: 38rpx; font-weight: 700; letter-spacing: 1rpx; }
.home__main-sub { font-size: 26rpx; opacity: 0.92; line-height: 1.45; }
.home__main-arrow { color: #b7b7c2; font-size: 60rpx; line-height: 1; opacity: 0.95; }

/* 副入口二宫格 */
.home__sub-entries {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 18rpx;
  margin-bottom: 36rpx;
}
.home__sub-entry {
  min-width: 0;
  background: rgba(255,255,255,0.92);
  border-radius: 24rpx;
  padding: 28rpx 0;
  text-align: center;
  box-shadow: 0 10rpx 28rpx rgba(31,31,41,0.05);
}
.home__sub-icon {
  width: 80rpx; height: 80rpx; border-radius: 20rpx;
  margin: 0 auto 14rpx;
  display: flex; align-items: center; justify-content: center;
  font-size: 40rpx; color: #fff;
}
.home__sub-icon--purple { background: #9b6cff; }
.home__sub-icon--orange { background: #f59e0b; }
.home__sub-icon--green  { background: #22c55e; }
.home__sub-label { font-size: 26rpx; color: #333; font-weight: 500; }

/* sections */
.home__section { margin-bottom: 36rpx; }
.home__section-head {
  display: flex; justify-content: space-between; align-items: center;
  margin-bottom: 20rpx; padding: 0 4rpx;
}
.home__section-title { font-size: 34rpx; font-weight: 600; color: #1a1a1a; }

/* 推荐网格：与款式库一致，用 CSS Grid 适配各机型（避免 calc+flex 在部分设备溢出） */
.home__grid {
  width: 100%;
}
.home__grid-item {
  min-width: 0;
  width: 100%;
}

.home__card {
  width: 100%;
  background: rgba(255,255,255,0.95);
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 10rpx 30rpx rgba(31,31,41,0.06);
}
.home__card-cover {
  position: relative;
  width: 100%;
  aspect-ratio: 3 / 4;
  background: #f0f0f5;
  overflow: hidden;
}
.home__card-img {
  width: 100%; height: 100%; display: block;
}
.home__card-emoji {
  font-size: 80rpx;
  position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%);
}
.home__card-heat {
  position: absolute; right: 12rpx; bottom: 12rpx;
  padding: 6rpx 12rpx; border-radius: 999rpx;
  background: rgba(255,255,255,0.88);
  box-shadow: 0 6rpx 18rpx rgba(0,0,0,0.08);
  font-size: 20rpx; color: #4a3a42; white-space: nowrap;
}
.home__card-body { padding: 16rpx 20rpx 20rpx; min-width: 0; }
.home__card-title {
  font-size: 28rpx; color: #1a1a1a; font-weight: 500;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
}
.home__card-rating {
  display: flex;
  align-items: center;
  margin-top: 10rpx;
  gap: 8rpx;
  min-width: 0;
  overflow: hidden;
}
.home__card-stars {
  display: flex;
  align-items: center;
  gap: 2rpx;
  line-height: 1;
}
.home__card-star {
  font-size: 22rpx;
  color: #e5e5e5;
  line-height: 1;
}
.home__card-star--full {
  color: #ff6b00;
}
.home__card-star--half {
  background: linear-gradient(90deg, #ff6b00 50%, #e5e5e5 50%);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}
.home__card-score {
  font-size: 24rpx;
  font-weight: 600;
  color: #ff6b00;
  line-height: 1;
}
.home__card-tags { margin-top: 10rpx; display: flex; flex-wrap: wrap; }

.home__tag {
  display: inline-flex; padding: 4rpx 16rpx; border-radius: 999rpx;
  background: #ffd8e3; color: #ff5e8a; font-size: 22rpx;
  margin-right: 8rpx; margin-bottom: 4rpx;
}
.home__tag--mini { font-size: 20rpx; padding: 2rpx 12rpx; }

/* 热门搜索 */
.home__hot-list { background: #fff; border-radius: 20rpx; padding: 8rpx 0; box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.04); }
.home__hot-item {
  display: flex; align-items: center;
  padding: 20rpx 24rpx; border-bottom: 1rpx solid #f0f0f5;
}
.home__hot-item:last-child { border-bottom: none; }
.home__hot-rank {
  width: 56rpx; height: 56rpx; border-radius: 12rpx;
  display: flex; align-items: center; justify-content: center;
  font-weight: 700; font-size: 28rpx; margin-right: 20rpx;
}
.home__hot-rank--top    { background: #ff5e8a; color: #fff; }
.home__hot-rank--normal { background: #f0f0f5; color: #888; }
.home__hot-body { flex: 1; }
.home__hot-word { font-size: 30rpx; color: #1a1a1a; font-weight: 500; margin-bottom: 6rpx; }
.home__hot-meta { display: flex; align-items: center; font-size: 22rpx; color: #888; }
.home__hot-heat { margin-left: 8rpx; }

.home__safe-bottom { height: 40rpx; }
