.login {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  padding-top: 160rpx;
  text-align: center;
  background: linear-gradient(180deg, #fff5f7 0%, #f7f7fb 100%);
}
.login__logo { font-size: 72rpx; font-weight: 700; color: var(--np-primary); }
.login__subtitle { font-size: 26rpx; color: var(--np-text-weak); margin-top: 16rpx; }
.login__hero {
  display: block;
  width: 360rpx;
  height: 360rpx;
  margin: 64rpx auto 72rpx;
  border-radius: 32rpx;
  background: #fff;
  box-shadow: 0 12rpx 40rpx rgba(255, 94, 138, 0.12);
}
.login__actions {
  margin-top: auto;
  padding-bottom: 48rpx;
}
.login__btn {
  min-height: 88rpx;
  margin-bottom: 24rpx;
}
.login__btn[disabled] { opacity: 0.85; }
.login__skip { color: var(--np-text-weak); font-size: 26rpx; }
.login__policy { color: var(--np-text-weak); font-size: 22rpx; margin-top: 80rpx; }

.profile-dialog {
  position: fixed;
  inset: 0;
  z-index: 20;
}
.profile-dialog__mask {
  position: absolute;
  inset: 0;
  z-index: 0;
  background: rgba(22, 18, 24, 0.38);
}
.profile-dialog__panel {
  position: absolute;
  left: 40rpx;
  right: 40rpx;
  bottom: 56rpx;
  z-index: 1;
  padding: 44rpx 36rpx 32rpx;
  border-radius: 36rpx;
  background: #fff;
  box-shadow: 0 24rpx 80rpx rgba(20, 12, 24, 0.18);
}
.profile-dialog__title {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--np-text);
}
.profile-dialog__desc {
  margin-top: 10rpx;
  color: var(--np-text-weak);
  font-size: 24rpx;
}
.profile-dialog__avatar-wrap {
  width: 160rpx;
  height: 160rpx;
  margin: 36rpx auto 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
}
.profile-dialog__avatar-wrap--hint {
  border: 4rpx solid rgba(255, 94, 138, 0.45);
  background: #fff5f7;
}
.profile-dialog__avatar-btn {
  display: block;
  width: 144rpx !important;
  min-width: 144rpx;
  max-width: 144rpx;
  height: 144rpx !important;
  min-height: 144rpx;
  max-height: 144rpx;
  margin: 0;
  padding: 0;
  line-height: 1;
  border-radius: 50%;
  overflow: hidden;
  background: transparent;
  border: none;
}
.profile-dialog__avatar-btn::after {
  display: none;
  border: none;
}
.profile-dialog__avatar-tip {
  margin-bottom: 20rpx;
  color: var(--np-primary);
  font-size: 24rpx;
  text-align: center;
}
.profile-dialog__avatar {
  display: block;
  width: 144rpx;
  height: 144rpx;
  border-radius: 50%;
  pointer-events: none;
}
.profile-dialog__nickname {
  height: 88rpx;
  margin-bottom: 12rpx;
  padding: 0 28rpx;
  border-radius: 999rpx;
  background: #f7f7fb;
  color: var(--np-text);
  font-size: 30rpx;
  text-align: center;
}
.profile-dialog__nickname-ok {
  margin-bottom: 20rpx;
  color: var(--np-success);
  font-size: 24rpx;
  text-align: center;
}
.profile-dialog__confirm {
  min-height: 88rpx;
}
.profile-dialog__cancel {
  padding-top: 26rpx;
  color: var(--np-text-weak);
  font-size: 26rpx;
}
