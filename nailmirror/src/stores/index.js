// Stores 汇总
const { userStore } = require('./user.store');
const { favoriteStore } = require('./favorite.store');
const { tryOnStore } = require('./try-on.store');

module.exports = { userStore, favoriteStore, tryOnStore };
